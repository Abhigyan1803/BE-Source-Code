package com.example.demo.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.example.demo.model.PreviousTermPaper;

public interface PreviousTermPaperRepository extends JpaRepository<PreviousTermPaper, Long>{

}
